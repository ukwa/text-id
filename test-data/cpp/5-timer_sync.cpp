#a <a>N
#a <a/a.a>N
# a 0N
//同步定时器 阻塞N
a a_a()N
{N
    //所有的a 程序都需要包含至少一个a_a,该类提供对a功能的访问。N
    a::a::a_a a;N
    //使用a_aN
    a::a::a_a a(a, a::a::a::a(0));N
    //等待定时器结束N
    a.a();N
N
    a::a << "a a a" << a::a;N
    a 0;N
}N
N
#aN
